对照 `docs/prd/营销H5-Look图及Look视频 PRD.md` 与 Figma（Look视频 `1810:24552`、Look图 `1810:25117`）后，下面这份可以直接转给产品和设计。结论先说：**不能按当前稿直接开工**，有几处会卡住交互和接口，需要他们先拍板改稿。

---

## 一、必须先改（会卡住开发）

### 1. Look 图：参考图 / 文案到底是「共用一份」还是「每个视角各一份」

**PRD 自己打架，设计和 PRD 也不一致。**

- PRD 写：「每个任务组**共用**一套已选参考图和具体描述文本」
- 同一节又写：加新视角时，描述栏**复用第一个任务的输入值**（这是「复制一份后可改」，不是共用）
- 设计：每个 Look 视角下面都有独立的「主服装参考 / 搭配服装参考 / 具体要求」

请产品二选一并改文案、改稿：

| 方案 | 含义 | 设计要怎么改 |
|---|---|---|
| A. 真共用 | 改视角 1 的参考图/文案，视角 2 一起变 | 参考图和输入框只出现一次，不要挂在每个视角下 |
| B. 复制后独立 | 新视角带出视角 1 的值，之后可分开改 | 维持现结构，删掉 PRD 里「共用一套」 |

不拍板无法做数据模型和提交接口。

### 2. 上装 / 下装两套角度清单，没有判定规则

PRD 和设计注解都列了【上装】【下装】两套（上半身 vs 下半身）。

未说明：

- 以**主服装品类**为准，还是用户手选？
- 主服是外套、搭配是裤子（设计稿就是这个组合）时，用哪一套？
- 连衣裙 / 套装 / 无法归类时怎么办？

设计里的「添加 Look 视角」弹窗只画了上装三项（正面 2 张 / 背面 2 张 / 侧面 4 张），**没有下装，也没有 8 个单角度**。录入页默认还是「正面全身+上半身」，和裤子搭配在一起不合理。

请产品给出判定规则；请设计按完整清单出弹窗（含滚动、分组标题），不要只放 3 个组合项。

### 3. 「一次最多 9 张」和组合项（2 张 / 4 张）怎么卡

PRD 只写了：总数 **>9** toast「本次任务最多生成 9 张」；**=9** 时隐藏「添加 Look 视角」。

没写清楚：

- 已有 8 张时，点「添加」再选「正面 2 张」：是点确定后 toast，还是弹窗里直接禁用 2 张 / 4 张选项？
- 已有 2 张时，「侧面 4 张」在稿里是**置灰**的，但 2+4=6 并未超 9，**置灰原因没有标注**（额度？互斥？未完成稿？）
- 从「正面 2 张」**切换**成「侧面 4 张」若会超 9，切的时候拦不拦？

请产品补规则；请设计把「可选 / 禁用 / toast」三种状态画全。

### 4. Look 视频创意方案：PRD 整段从 Look 图复制错了

PRD 3.2.5 写：标题是「**具体角度**」，组合还要展开成具体角度。  
Look 视频本期**没有角度**（只做「细节图生 Look 视频」）。

设计是对的：标题为「Look视频」，正文为具体要求。

请产品删掉 3.2.5 的角度逻辑，改成与设计一致。空文案是否仍用「无要求」请一并写死。

### 5. Look 图「查看创意 / 修改要求」画面复用了视频模板

Look 图节点里，「查看创意」「修改要求」底层仍是：

- 交付物名称：`交付物(9:16)`
- 底栏：`查看视频操作栏`

这是视频页组件，不是图文九宫格/单图预览。和「图片详情 / 图片总览」对不上。

请设计换成图文查看器；总览态、单图态分开画。PRD 3.1.6.2（总览里只展示角度+要求、**不展示素材图**）在这两帧里**没有对应稿**。

---

## 二、PRD 内部问题（像复制粘贴残留）

1. **3.1.2**：「图文广场 / 我的视频/图文」里新增「Look视频/Look图」。Look 视频不应进图文 Tab。请拆成：图文侧只加 Look 图；视频侧只加 Look 视频（3.2.2 已写 Look 视频，和 3.1.2 重复且打架）。
2. **3.1.5 设计注解**写：「组合名称仅用于发起任务时的 **Look 视频** 选择」。这是 Look **图**弹窗，应为 Look 视角/Look 图。
3. **3.3** 只提「所有图文 AIGC」失败弹窗加不可改标题。Look 图多角度时标题用组合名还是单角度名？失败弹窗这两帧**没有设计**。
4. **校验 4.2**「其他参数均有默认及最小值可不校验」vs **3.2.4**「参考图至少选 1 张」。用户把默认正反面两张全部取消时，提交拦不拦？请写死。
5. **字数**：录入 100 字，修改/重新生成 150 字。若故意两套请注明；否则统一，避免和「自由场景」再不一致。
6. **1 SKU 已选超过 4 张再加搭配服**：主服参考**静默恢复成初始 2 张**。已选细节会丢，没有确认。请产品确认要不要二次确认。
7. **「做同款」带入所有值**：多视角 Look 图要带哪些（角度列表、每组参考、每组文案、画幅、清晰度）没写。首页/Tab/做同款也不在你给的两帧里，需要产品指对应节点或补稿。

---

## 三、设计内部不一致（同一文件里就对不上）

| 点 | 问题 |
|---|---|
| 模块标题 | 录入页是「Look视频」；出镜人物状态稿是「Look展示」；回顾弹窗类型标签也是「Look展示」 |
| 角度名称 | 录入页「正面组合(2图)」；弹窗/PRD「正面2张 (正面全身+上半身)」；查看创意标题「正面全身图」。三套文案 |
| 单视角操作 | 状态稿第一个视角带**删除**；完整录入页是**切换**。PRD：至少一个视角，仅一个时只能切换、不能删光 |
| Look 视频画幅 | 录入页默认 **9:16**；「正在生成-回顾方案」顶栏却是 **Look展示 + 3:4 + 720P**（查看创意里这栏又隐藏） |
| 参考图上限文案 | 「出镜人物-选用了参考图」仍是 1 个 SKU，文案却写成「主服装参考 (最多4张)」。4 张上限只属于 2 个 SKU |
| 选择参考图弹窗 | Look 图弹窗底下还露出「品牌 LOGO」模块。Look 图 PRD 没有品牌 LOGO，属于套错视频页 |

---

## 四、体验 / 规则缺口（建议产品补一句）

1. **重复选同一角度**（PRD 允许）：同一「正面全身」交两次，平台是否接得住、广场卡片怎么展示，需要和后台确认。
2. **多视角时能不能「切换」**：PRD 只说「仅一个时可切换，多个时可删」。设计多视角是垃圾桶。请明确：多个时是只能删了重加，还是每个都能切换。
3. **Look 图修改要求**：设计「请选择 1 张需要优化的参考图(可选)」。图来自服装细节、已生成图，还是混在一起？PRD 说图生图，但没说候选池。
4. **Look 视频没有「按剩余额度置灰」**，这点合理；不要把 Look 图的 9 张逻辑套到视频上。
5. **后台「同现有流程」**过粗：H5 一次提交多个角度任务组，是一个父任务打多个子任务，还是多次创建？影响轮询、失败重试、做同款。需要产品和后端给接口口径，前端才能估工期。

---

## 五、这两帧对得上、可以先当基线的部分

- Look 视频录入：出镜人物（1 人 + 主服必选 + 搭配可选）→ 主/搭配参考图（1 SKU 最多 6、2 SKU 各最多 4，加第二件回退到 2 张）→ 具体要求 100 字 → 品牌 LOGO → 画幅 3:4 / 9:16 → 清晰度 720P / 1080P → 生成视频。
- Look 图录入：无品牌 LOGO；画幅在原有 3:4、9:16 上增加 1:1、2:3；清晰度 2K / 4K；底栏「生成图片(N张)」。
- 参考图弹窗最多 18 张、勾选即时生效、超上限 toast「最多选择 N 张参考图」。
- Look 图单图「查看创意」标题用具体角度（如「正面全身图」），素材：首行头像+SKU，下方主服/搭配参考组。

---

请产品和设计优先回这 5 个问题：**共用还是复制、上装下装怎么判、9 张如何卡组合项、Look 视频创意标题、Look 图查看/总览/失败不要复用视频页。** 定稿后再开工，避免按错误契约做一版再推翻。